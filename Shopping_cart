foods=[]
prices=[]
total=0

while True:
    food=input("enter the food item(or q to exit): ")
    if food.lower()=="q":
        break
    else:
        price=float(input(f"enter the price of the {food}: $"))
        foods.append(food)
        prices.append(price)
print("----- Your cart-----")
for food in foods:
    print(food)
for price in prices:
    total += price

print(f"your total is ${total}")
